
#服务端口号
server:
  port: 8004
spring:
  application:
    name: consul-provider-payment

#consul注册中心的地址
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}    #localhost: 127.0.0.1
